#include "shell.h"
#include <stdio.h>

/**
 * main - entry point
 * Return: current environment
 */
extern char **env;
int main(void)
{
	if
	(int j = 0; env[j] != NULL; j++)
	{
	printf(("%s\n" env[j]));
	}
	else
	return (0);
}
